pandas>=1.3.0
numpy>=1.21.0
markdown>=3.4.0

# 可选依赖（不安装也能正常工作）
# yfinance>=0.2.30    # 美股备用数据源（当前使用东方财富 API，无需安装）
# weasyprint>=59.0    # PDF 导出（不安装时自动降级为 HTML）
# akshare>=1.13.0     # AKShare 备选数据源（财务/公司/新闻数据的第三个独立来源）

